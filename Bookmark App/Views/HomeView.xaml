<UserControl x:Class="Bookmark_App.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Bookmark_App.ViewModels"
             xmlns:views="clr-namespace:Bookmark_App.Views"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Background="#1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="15*" 
                           MinHeight="60"/>
            <RowDefinition Height="85*" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#292929" MinHeight="60">
            <Viewbox Stretch="Uniform" StretchDirection="Both" VerticalAlignment="Center" HorizontalAlignment="Left">
                <StackPanel Orientation="Horizontal" Margin="0">
                    <TextBlock Text="Home"
                       Foreground="White"
                       FontSize="28"
                       FontWeight="Bold"
                       HorizontalAlignment="Left"
                       Margin="10,0,0,5"/>
                </StackPanel>
            </Viewbox>
        </Border>
        <Border Grid.Row="1" MinHeight="60">
            <Grid>
                <!-- Lege-staat tekst -->
                <TextBlock Text="Er zijn geen lijsten aanwezig. Druk op de plus-knop in de zijbalk."
                           FontSize="18"
                           Foreground="#AAAAAA"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Margin="24">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <!-- standaard verborgen -->
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <!-- Als Lists.Count == 0 → tekst tonen -->
                                <DataTrigger Binding="{Binding Lists.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- Lijstweergave -->
                <Viewbox Stretch="Uniform"
                         StretchDirection="Both"
                         VerticalAlignment="Top"
                         HorizontalAlignment="Left">
                    <ItemsControl ItemsSource="{Binding Lists}">
                        <ItemsControl.Style>
                            <Style TargetType="ItemsControl">
                                <!-- standaard zichtbaar -->
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <!-- Als Lists.Count == 0 → lijst verbergen -->
                                    <DataTrigger Binding="{Binding Lists.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ItemsControl.Style>

                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="3" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <views:ListCardView/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Viewbox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
